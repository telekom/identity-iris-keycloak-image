# Keycloak themes

Each theme is in its own folder. The `build.sh` script builds the themes into jar files.

For more information visit: https://www.keycloak.org/docs/9.0/server_development/#creating-a-theme